import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  frame: {
    width: '100%',
    height: 100,
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
  },
  button: {
    width: '90%',
    justifyContent: 'center',
    alignItems: 'center',
  },
});
